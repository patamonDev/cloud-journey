Current structure outline:
  - Split into two environments, dev and prod
  - Each module will create a specific resource for each env
  - Once I have the Azure cloud infra laid out and configured, will create an AWS equivalent to simulate multi-cloud tenancy
